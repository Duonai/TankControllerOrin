{
  "type": "object",
  "required": ["role", "result"],
  "additionalProperties": false,
  "properties": {
    "role": {
      "type": "string",
      "enum": ["player3_voice"]
    },
    "result": {
      "type": "object",
      "required": ["command", "data"],
      "additionalProperties": false,
      "properties": {
        "command": {
          "type": "string",
          "enum": [
            "move_forward",
            "move_backward",
            "pivot_left",
            "pivot_right",
            "turret_rotate_left",
            "turret_rotate_right",
            "turret_up",
            "turret_down",
            "reload",
            "scanning",
            "fire",
            "reject"
          ]
        },
        "data": {
          "type": "number"
        }
      }
    }
  }
}
